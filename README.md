


### Building

```
git clone https://github.com/homer6/sentence-transformer.git
cd sentence-transformer

#sudo apt install libtorch-dev

cd deps/torch
git submodule update --init --recursive --jobs 0

```
